// examples/demo.ltl — runnable demo for ltl_mini_shell

import std::io
use ltl_mini_shell as lib

fn main() {
    io::println("ltl_mini_shell demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}